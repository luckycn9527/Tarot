# E-Tomd 博客功能需求（MVP PRD）

## 1. 产品目标

博客不是独立的新闻页，而是由塔罗师持续创作的内容入口：

`搜索/首页发现 -> 认识塔罗师 -> 阅读学习/交流/经验 -> 进入相关牌阵或占卜 -> 通过新文章回访`

首期目标：

- 用高质量塔罗、雷诺曼和自我反思内容获得自然搜索流量。
- 帮助用户在开始占卜前理解牌义、牌阵和提问方式。
- 每篇文章都能自然导向一个相关功能，而不是把阅读体验变成硬广告。
- 让塔罗师能以自己的身份创作，管理员能安全地完成审核、预览、发布和撤回。

## 2. 用户与权限

| 角色 | 能力 |
| --- | --- |
| 未登录访客 | 浏览列表、分类、标签、搜索、阅读已发布文章、分享文章 |
| 登录用户 | 在访客能力上，关注塔罗师（第二阶段）、保存文章、进入关联占卜 |
| 塔罗师作者 | 使用已认证的塔罗师身份提交文章草稿，维护自己的作者资料和文章 |
| 内容编辑（管理员） | 代表/审核塔罗师创建或编辑草稿、预览、上传封面、设置 SEO、发布/定时发布、撤回、归档 |
| 超级管理员 | 内容编辑能力 + 删除/恢复、查看审计记录、管理编辑权限 |

MVP 不开放普通用户投稿、公开作者注册和评论；塔罗师作者由管理员建立或审核邀请，先控制内容质量与交流成本。

## 3. 内容模型

### 3.1 文章字段

- `id`：内部主键。
- `slug`：公开 URL 标识，只允许小写字母、数字和连字符，唯一且不可默默复用。
- `title`：中文标题，长度 5-80 字。
- `excerpt`：列表摘要，长度 30-220 字。
- `body`：富文本内容。推荐 Markdown 或受限 JSON 文档；服务端统一转换并清洗后再输出。
- `cover_url`：OSS 图片 URL；必须是允许的 OSS/CDN 域名。
- `author_reader_id`：关联现有塔罗师/作者档案，必须来自受信任的作者表。
- `author_name`、`author_avatar_url`：作者展示信息，由作者档案生成，文章内不允许随意伪造。
- `author_bio`、`author_specialties`：作者简介与擅长领域，可在作者页复用。
- `author_statement`：文章级署名说明，例如“本篇由 XX 塔罗师撰写”。
- `content_type`：`learning`、`discussion`、`experience`，对应学习、交流、经验。
- `category`：单选主分类。
- `tags`：多选标签，最多 8 个。
- `related_cards`：关联塔罗/雷诺曼牌 ID，最多 6 个。
- `related_routes`：关联功能路由，例如 `/tarot`、`/oracle-gallery`，最多 3 个。
- `status`：`draft`、`review`、`scheduled`、`published`、`archived`。
- `published_at`、`scheduled_at`、`created_at`、`updated_at`。
- `reading_minutes`：按正文长度自动计算，允许管理员覆盖。
- `seo_title`：可选，默认使用标题，长度不超过 60 个字符。
- `seo_description`：可选，默认使用摘要，长度不超过 160 个字符。
- `canonical_url`、`og_image_url`：可选，需经过 URL 白名单校验。

### 3.2 作者档案

每位塔罗师应有公开作者档案：头像、显示名、个人简介、擅长主题、从业/学习经历（可选）、已发布文章数和作者页 URL。

作者档案必须区分“平台认证信息”和“作者自述”：认证状态由管理员维护，自述内容需要长度限制与审核，不展示私人邮箱、手机号或内部账号信息。

### 3.3 内容主题与分类建议

首期控制在 5 个以内：

1. 塔罗学习：牌义、正逆位、元素、宫廷牌、提问方式、练习方法。
2. 塔罗交流：不同流派、解牌思路、案例讨论、读牌中的分歧与边界。
3. 塔罗经验：塔罗师的实战复盘、咨询伦理、沟通经验、长期学习记录。
4. 雷诺曼与神谕：雷诺曼牌、神谕牌和跨体系学习。
5. 牌阵与自我反思：牌阵指南、记录方法、复盘练习。

分类用于导航和 SEO；标签用于细分主题，例如“爱情”“事业”“逆位”“每日练习”“案例复盘”。每篇文章必须明确属于“学习 / 交流 / 经验”中的至少一个主题方向。

### 3.4 塔罗师写作规范

- 使用塔罗师第一人称署名表达，例如“在我的读牌练习中”，不能伪装成平台统一结论。
- 学习类文章要有明确学习目标、核心概念、示例和可执行练习，必要时标注参考书目或资料来源。
- 交流类文章允许呈现不同流派和解释分歧，必须区分“传统定义”“作者理解”和“个案经验”。
- 经验类文章应说明适用背景、过程、反思和局限，不把单个案例包装成普遍规律。
- 案例必须去标识化，不出现可识别的姓名、联系方式、单位、聊天截图或未经授权的咨询原文。
- 不使用“必然”“百分百”“注定”等绝对化预测；不鼓励用户用重复占卜替代现实沟通和专业求助。
- 涉及健康、心理危机、法律、投资或人身安全时，只提供反思视角，并引导用户寻求相应专业支持。
- 每篇文章至少提供一个真实价值点：知识框架、练习方法、案例复盘或可讨论的问题，不能只是导流文案。

## 4. 用户端页面

### 4.0 视觉与内容方向

- 页面类型：编辑型博客，而不是营销落地页或卡片式仪表盘。
- 目标用户：正在学习塔罗/雷诺曼，或希望先理解问题再开始占卜的中文用户。
- 视觉参数：差异度 6、动效 4、信息密度 3。
- 延续现有深色背景、金色品牌点缀和现有字体体系；正文使用更高对比度和更稳定的行宽。
- 动效只用于页面进入、目录定位、阅读进度和按钮反馈；遵循 `prefers-reduced-motion`。
- 不把文章正文装进大卡片，不使用连续嵌套卡片，也不让装饰抢过标题、封面和正文。
- 封面必须表达文章真实主题，例如牌面、牌阵、占卜桌或作者制作的图解，不使用模糊氛围图充数。

### 4.1 博客首页 `/blog`

- 首屏展示 1 篇精选文章，必须有真实封面、标题、摘要、分类和阅读时长。
- 下方按发布时间展示文章流，使用分页或“加载更多”，不使用无限滚动作为唯一入口。
- 支持分类筛选、标签筛选和关键词搜索。
- 提供空状态、加载状态、错误状态和无结果状态。
- 侧栏/移动端区域展示热门标签、最新文章和“开始一次占卜” CTA。
- 文章卡片保持纵向窄版比例，避免宽而扁的营销卡片。

### 4.2 文章详情 `/blog/:slug`

- 显示标题、摘要、作者、发布日期、更新时间、分类、阅读时长和封面。
- 正文内容区限制在适合阅读的宽度（约 680-760px），桌面端可提供目录。
- 目录只取 `h2/h3`，标题必须生成稳定锚点；移动端可折叠。
- 提供阅读进度、返回博客、分享、复制链接和上一篇/下一篇。
- 文章末尾展示最多 3 个相关内容：关联文章、牌面详情、占卜入口。
- 医疗、法律、投资等高风险内容必须显示“仅供自我反思，不构成专业建议”提示。

### 4.3 塔罗师作者页 `/blog/authors/:authorSlug`

- 展示塔罗师头像、认证标识、简介、擅长领域和已发布文章。
- 文章列表支持按“学习 / 交流 / 经验”筛选。
- 作者页只展示公开资料，不暴露邮箱、手机号、内部 ID 或未发布内容。
- 在作者页提供“开始一次占卜”入口，但 CTA 必须指向该作者实际支持的读牌功能。

### 4.4 SEO 与分享

- 每篇已发布文章输出唯一 canonical URL。
- 输出 `Article` JSON-LD：`headline`、`description`、`image`、`author`、`datePublished`、`dateModified`、`mainEntityOfPage`。
- 更新 sitemap，加入已发布文章；草稿、归档和预览链接不得进入 sitemap。
- 提供 RSS/Atom（可放到第二阶段）。
- 分享卡片使用 `og:title`、`og:description`、`og:image` 和 `twitter:card`。
- 文章 URL 变更时保留旧 slug 重定向，避免 SEO 断链。

## 5. 管理端流程

### 5.1 编辑流程

1. 塔罗师新建草稿，系统自动绑定当前作者身份。
2. 填写标题、摘要、正文、主题方向、分类、标签和关联功能。
3. 上传封面到 OSS，生成预览图和备用文本。
4. 自动计算阅读时长并生成 SEO 默认值。
5. 保存草稿。
6. 在独立预览页检查桌面端、移动端、目录、链接和分享信息。
7. 塔罗师提交审核，状态变为 `review`；管理员可以退回修改并填写原因。
8. 管理员审核通过后立即发布或设置发布时间。
9. 发布后可撤回到草稿/归档；已发布文章保留修订记录。

### 5.2 管理端列表

- 按状态、分类、作者、主题方向和更新时间筛选。
- 显示标题、状态、发布时间、更新时间、阅读时长和最近编辑者。
- 支持批量归档，但不提供无确认的永久删除。
- 所有发布、撤回、删除、slug 修改和封面替换写入管理员审计日志。
- 退回审核必须记录原因，作者在自己的工作台可见。

## 6. API 需求

### 6.1 公开接口

- `GET /api/blog/posts?page=1&pageSize=12&category=&tag=&q=`
  - 只返回 `published` 文章。
  - 返回分页元数据、摘要和封面，不返回完整正文。
- `GET /api/blog/posts/:slug`
  - 只返回已发布文章；支持 `Cache-Control`。
  - 返回正文、目录数据、相关文章和关联功能。
- `GET /api/blog/categories`
- `GET /api/blog/tags`
- `GET /api/blog/authors`
- `GET /api/blog/authors/:authorSlug`

### 6.2 管理接口

- `GET /api/admin/blog/posts`
- `POST /api/admin/blog/posts`
- `GET /api/admin/blog/posts/:id`
- `PUT /api/admin/blog/posts/:id`
- `POST /api/admin/blog/posts/:id/publish`
- `POST /api/admin/blog/posts/:id/unpublish`
- `POST /api/admin/blog/posts/:id/approve`
- `POST /api/admin/blog/posts/:id/reject`
- `POST /api/admin/blog/posts/:id/preview-token`
- `GET /api/admin/blog/posts/:id/revisions`
- `POST /api/admin/blog/posts/:id/restore/:revisionId`
- `GET /api/author/blog/posts`
- `POST /api/author/blog/posts`
- `GET /api/author/blog/posts/:id`
- `PUT /api/author/blog/posts/:id`
- `POST /api/author/blog/posts/:id/submit`
- `GET /api/author/profile`
- `PUT /api/author/profile`
- 复用现有管理员上传接口，但必须增加用途、尺寸和域名校验。

管理接口必须使用现有 `adminAuth`、写操作限流、审计日志和 Zod 校验；作者接口必须使用作者身份校验、资源归属校验、写操作限流和 Zod 校验。

## 7. 数据库建议

建议新增：

- `blog_posts`
  - 唯一索引：`slug`
  - 查询索引：`status, published_at`、`category, published_at`
  - 正文使用 `LONGTEXT` 或 JSON，摘要/SEO 字段使用受限长度。
- `blog_author_profiles`
  - 与现有塔罗师/用户身份建立唯一关联。
  - 保存公开 slug、简介、擅长领域、认证状态和公开头像。
- `blog_tags`
- `blog_post_tags`
- `blog_post_revisions`
- `blog_post_redirects`

不要把标签、关联牌和修订记录塞进一个不可查询的大 JSON 字段，否则后续筛选、统计和迁移会困难。

## 8. 安全与可靠性要求

- 正文禁止直接信任 `v-html`；服务端必须使用白名单清洗 HTML/Markdown 输出。
- 禁止 `javascript:`、`data:`（图片之外）、内联事件处理器和任意 iframe。
- 图片上传只允许管理员；校验 MIME、真实文件格式、尺寸、像素总量和文件大小，统一转 WebP 并上传 OSS。
- 所有 slug、分页、分类、标签和搜索参数做 schema 校验；SQL 只使用参数绑定。
- 预览使用短时效、一次性 token，不让未发布正文通过可猜 ID 访问。
- 作者只能读取、修改和提交自己名下的文章；管理员审核接口必须再次校验文章 ID 与作者归属。
- 作者认证状态变化后，已发布文章保留历史署名快照，避免作者资料变更导致旧文章署名失真。
- 草稿、修订、作者邮箱、内部备注不得通过公开接口返回。
- 公开文章接口按 IP 限流并设置缓存；搜索限制长度和结果量，防止昂贵模糊查询。
- 发布与撤回必须具备幂等性；定时发布任务重复执行不能重复生成文章或重复写入审计记录。
- 文章中涉及健康、财务、法律、心理危机的内容需要编辑审核标记和免责声明。
- 删除文章默认软删除，保留恢复能力和重定向记录。

## 9. MVP 与后续版本

### MVP 必须有

- 公开博客列表、详情、分类、标签、搜索、分页。
- 塔罗师作者档案、作者页、作者草稿/提交审核。
- 管理员草稿/编辑/预览/审核/发布/撤回。
- OSS 封面上传、文章 SEO、Article JSON-LD、sitemap 接入。
- 相关文章和关联占卜 CTA。
- 输入清洗、权限校验、审计日志、限流、缓存。

### 第二阶段

- 定时发布任务、修订恢复、RSS、收藏、阅读进度同步。
- 文章系列、关注塔罗师、邮件订阅和内容数据分析。

### 暂不做

- 普通用户评论、社区点赞、开放用户投稿、实时讨论区、复杂所见即所得协作编辑器。
- 付费文章和会员专属内容，除非先定义支付、退款、权限过期和客服流程。

## 10. 验收标准

- 未登录用户可在 3 次点击内从首页进入博客并打开文章。
- 列表首屏在移动端不横向溢出，封面、标题和摘要不会重叠或跳动。
- 草稿不会出现在公开列表、搜索、sitemap、RSS 或 JSON-LD 中。
- 同一 slug 无法创建第二篇文章；修改 slug 会生成旧 URL 重定向。
- 文章正文中的脚本、事件属性、危险 URL 在保存和输出阶段均被移除。
- 非管理员无法读取草稿、修订和预览正文。
- 发布、撤回、删除和恢复均可在审计日志中追溯。
- 公开详情页具备正确的标题、canonical、Article JSON-LD、OG 图片和更新时间。
- 每篇公开文章都展示可点击的塔罗师署名，并能进入对应作者页；公开接口不能伪造或覆盖作者身份。
- 后端单元测试覆盖权限、slug 冲突、清洗、发布幂等、分页和预览 token；浏览器 E2E 覆盖访客阅读和管理员发布闭环。
- 浏览器 E2E 还必须覆盖“塔罗师提交 -> 管理员退回 -> 塔罗师修改 -> 管理员通过 -> 公开可见”的完整闭环。

## 11. 参考规范与成熟产品能力

- Google Search Central：Article structured data
  - https://developers.google.com/search/docs/appearance/structured-data/article
- WordPress REST API：Posts 资源的状态、分类、标签、发布日期和分页模型
  - https://developer.wordpress.org/rest-api/reference/posts/
- Ghost：编辑、草稿、发布与内容 API 的常见内容工作流
  - https://ghost.org/help/publishing/
  - https://docs.ghost.org/content-api/posts
- Schema.org Article 类型
  - https://schema.org/Article

这些资料用于抽取通用能力；E-Tomd 的首期范围仍以“塔罗/神谕内容 + 相关占卜转化 + 管理员可控发布”为准，不照搬社区评论或复杂 CMS 能力。
