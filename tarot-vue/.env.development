# Vite 代理目标（/api、/uploads）；publicAssetUrl 在 DEV 下用相对路径走代理，避免跨端口 CORP 拦截
VITE_PUBLIC_API_ORIGIN=http://localhost:5174

# 可选：canonical / og:url 站点根（不设则用浏览器 origin）
# VITE_PUBLIC_APP_ORIGIN=http://localhost:5175
